import React from "react";

const UserForm = (props) => {
    /* 
    1. Create data structure here
    2. Create the form. 
    3. The state of the form changes and then passed to the new User component. 

    */

    return <form>
    </form>
}

export default UserForm;